[package]
name = "zwift-routes"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
description = "Zwift route + curve tables and route-progress computations (port of sauce4zwift's shared/routes.mjs + shared/curves.mjs)."

[lib]
doctest = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
approx = "0.5"
